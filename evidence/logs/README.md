# Logs

The original organized package did not contain all raw UART transcripts. The recovered RC7 direct-install transcript is retained under `../openwrt/v1.16.0-rc7/`, while older OpenWrt run evidence is normalized under `../openwrt/toolkit-v1.10.3/`.

Future UART, TFTP, kernel, and build logs should be stored here by date and test-session identifier.
