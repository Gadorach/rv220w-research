# Source material

`urls.csv` is the reference index. Third-party documents are not committed because redistribution terms differ.

Run:

```sh
./sources/fetch-public-docs.sh
```

Downloaded files are written to `sources/downloads/`, which is ignored by Git.
