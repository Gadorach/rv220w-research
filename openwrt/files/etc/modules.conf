# Derive the BCM4322 WLAN identity from the protected factory MAC stored in
# the boot-chain partition.  The hardware-qualified b43 path uses native DMA.
options b43 macaddr_mtd=boot-chain macaddr_mtd_offset=0x6ff00 macaddr_increment=4
