# Installation

The combined boot-policy patch must be applied before automatic OpenWrt slot
promotion. Use the canonical procedure:

[`../../docs/openwrt/installation.md`](../../docs/openwrt/installation.md)

The toolkit writes only `openwrt-slot`. It does not patch U-Boot or save the
environment automatically.
