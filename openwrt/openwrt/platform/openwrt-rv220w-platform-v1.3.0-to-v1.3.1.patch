diff --git a/target/linux/octeon/files/arch/mips/boot/dts/cavium-octeon/cn5010_cisco_rv220w.dts b/target/linux/octeon/files/arch/mips/boot/dts/cavium-octeon/cn5010_cisco_rv220w.dts
--- a/target/linux/octeon/files/arch/mips/boot/dts/cavium-octeon/cn5010_cisco_rv220w.dts	2026-07-24 23:20:50.984792415 +0000
+++ b/target/linux/octeon/files/arch/mips/boot/dts/cavium-octeon/cn5010_cisco_rv220w.dts	2026-07-24 23:20:50.984792415 +0000
@@ -20,7 +20,7 @@
 	};
 
 	aliases {
-		/* Octeon early DT fixup scans uartN aliases to supply the I/O clock. */
+		/* Retain both conventional aliases for userspace and early console naming. */
 		uart0 = &uart0;
 		serial0 = &uart0;
 		pip = &pip;
@@ -43,6 +43,8 @@
 };
 
 &uart0 {
+	/* CN5010 I/O/UART clock equals the verified 400 MHz core clock. */
+	clock-frequency = <400000000>;
 	status = "okay";
 };
 
@@ -91,11 +93,10 @@
 };
 
 /*
- * Populate only the CS0 CFI child. The Octeon flash driver reads the active
- * BootBus CS0 hardware register established by U-Boot and derives the true
- * 0x1dc00000 base, 32 MiB size, and x16 bank width from that register. The
- * unrelated generic-board timing, display, and CompactFlash children are
- * deleted so Linux cannot reconfigure or probe them.
+ * Keep the BootBus parent active so the Octeon platform flash setup can read
+ * U-Boot's live CS0 register and register the verified 32 MiB x16 NOR at
+ * 0x1dc00000. Delete every inherited generic-board child, including the
+ * incorrect 0x1f400000 physmap node, so only phys_mapped_flash is registered.
  */
 &bootbus {
 	status = "okay";
@@ -106,8 +107,5 @@
 	/delete-node/ cavium,cs-config@6;
 	/delete-node/ led-display@4,0;
 	/delete-node/ compact-flash@5,0;
-
-	nor@0,0 {
-		status = "okay";
-	};
+	/delete-node/ nor@0,0;
 };
