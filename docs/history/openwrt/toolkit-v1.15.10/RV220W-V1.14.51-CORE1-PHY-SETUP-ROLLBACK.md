# RV220W v1.14.51 — core1 PHY setup rollback qualification

## Qualified predecessor

v1.14.50 hardware-qualified fault point 7 (`core0-iq`) on CA channel 8. The run repeated the qualified core0 gain search, generated the corrected dedicated complex tone, completed the explicit 16384-sample coefficient calculation with raw I/Q/IQ values 137674147 / 141165210 / -1633432, reported calculation error 0 and applied mask 0x01, observed candidate A0=0/B0=2, then reached the intended MIPS `-ECANCELED` and restored the original coefficient tuple exactly with cleanup rc/mask zero.

## v1.14.51 boundary

v1.14.51 adds only fault point 8 (`core1-phy`). The transaction executes the already-qualified core0 gain and IQ-calculation path, restores core0 radio/PHY state, performs the existing rev4 core1 PHY setup, captures `B43_NPHY_RFSEQCA`, then injects cancellation immediately.

No core1 BCM2056 radio setup, core1 gain search, internal tone, dedicated IQ calculation, RCCAL sweep/commit, association/AP/user-data TX, frame injection or persistent write is reachable from this public gate.

For pre-rev7 core1 RX with opposite core0 TX, the Broadcom core-bitmap semantics produce:

```text
RXDIS = BIT(0) << 12 = 0x1000
TXEN  = BIT(0)       = 0x0001
RXEN  = BIT(1) << 4  = 0x0020
TXDIS = BIT(1) << 8  = 0x0200
RFSEQCA              = 0x1221
```

The kernel diagnostic, target helper and analyzer all require live `RFSEQCA=0x1221` for point8 qualification.

## Qualification requirements

A PASS requires the complete predecessor core0 gain/IQ gates, point8 actual ID, live/expected core1 RFSEQCA `0x1221`, architecture-resolved `-ECANCELED`, cleanup rc 0/mask 0, verified MAC suspend/resume and exact before/after coefficient equality.

If point8 passes, the next bounded candidate is point9 after core1 BCM2056 radio setup. Normal/full RX-IQ calibration remains blocked.
