# RV220W v1.14.72 regression-version coherence hotfix

The real v1.14.71 updater run proved the v1.14.71 source-marker compatibility fix: the historical RX-IQ chain advanced through v1.14.58. It then stopped in `test_v11459_rxiq_retained_live_state.py` because that test's root `VERSION` assertion had been advanced while its `toolkit.json` assertion was still pinned to `1.14.69`.

A full sweep found the same split assertion in v1.14.60 and v1.14.61. v1.14.72 advances those three metadata assertions, advances all normal current-tree test version assertions to 1.14.72, and adds `test_v11472_regression_version_coherence.py` as an updater preflight. The preflight rejects stale direct current-tree `VERSION`/`toolkit.json` version assertions before the long historical chain runs.

There is no runtime change. The b43 platform patch, RV220W PCI-INT0 mapping patch, retained calibration, IRQ109/CIU36 receive path, active-scan gate, persistent platform v1.7.5, and all NVM/NOR boundaries are unchanged.

A second sweep-time failure showed `test_v11461_rxiq_operational_capture_hotfix.py` also required the current policy file to retain `operational_rxiq_receiver_qualified=0`. That was a historical/current-state coupling error: v1.14.69 has hardware-qualified the receiver. The test now requires preserved v1.14.61 result provenance while accepting and requiring the current qualified receiver state.
