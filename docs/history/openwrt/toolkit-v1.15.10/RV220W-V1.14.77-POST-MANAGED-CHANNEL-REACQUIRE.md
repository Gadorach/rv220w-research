# RV220W v1.14.77 — post-managed channel-8 reacquire

The v1.14.76 OpenWrt build and RAM boot succeeded, proving the corrected post-998 patch stacks and compiles. The active-scan run still stopped before TX, but the generation counter made the reason unambiguous: the first `verify:retained` reached generation 2, while the post-managed write returned nonzero and generation stayed 2. The second command therefore never entered the kernel verifier; its displayed success fields were stale from the first verification.

`verify:retained` is guarded by started N-PHY rev4/BCM2056r3 state, channel 8, PIO, fault point 15, retained-state verification and integration completion. Managed-vif UP is the first step that permits mac80211 to take ownership of the channel context, making a channel transition the leading hardware hypothesis.

v1.14.77 keeps the qualified 993/998 patches and v1.14.76 999 patch byte-identical. The target helper now captures every relevant guard immediately after managed-vif UP, then issues `iw dev rvwactmon0 set freq 2447`, requires monitor readback 2447 MHz and b43 channel readback 8, and only then requests the fresh retained verification. Failure to reacquire is classified independently from a fresh-verification rejection. No scan occurs until all calibration/channel/generation gates pass.
