# RV220W v1.14.63 — regression-suite/updater hotfix

The v1.14.61→v1.14.62 updater successfully installed the v1.14.62 toolkit and passed manifest, platform-v1.7.5, and configuration-preservation checks, but then aborted in `test_v11435_stock_rxiq_dispatch_correction.py`. That historical test still required the exact v1.14.61 collector warning banner. Eleven additional historical tests contained the same stale assertion, and the v1.14.32 metadata regression still hard-coded `CURRENT == 1.14.61`.

v1.14.63 fixes those regression contracts. Historical tests now assert the stable `RX-path localization requested` behavior rather than a particular release-number banner, and the metadata regression derives the active release from `VERSION`/`toolkit.json`.

The b43 patch is byte-for-byte identical to v1.14.62. No receiver, calibration, RCCAL, association/TX, or persistence boundary changes.
