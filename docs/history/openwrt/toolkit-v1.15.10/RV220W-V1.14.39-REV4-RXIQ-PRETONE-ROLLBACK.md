# RV220W v1.14.39 — rev4 RX-IQ pre-tone rollback qualification

v1.14.38 compiled and RAM-booted the complete target-scoped rev4 outer transaction with the normal run gate closed. v1.14.39 opens only a separate fault-injection diagnostic for five checkpoints that occur before the first gain-control or calibration-tone operation.

Allowed channel-8 stages: `carrier`, `tpc-off`, `gain-table`, `core0-phy`, `core0-radio`.

A test passes only when the kernel observes the expected injected `-ECANCELED`, reports `cleanup_rc=0` and `cleanup_fail_mask=0`, restores the IQ tuple exactly, and the debugfs caller verifies MAC suspend/resume. No scan, association, AP, frame injection, user-data TX, SPROM/NOR/NVM write, gain search, RCCAL or calibration tone is requested.

Normal `rxiq-calibration` remains blocked.
