# RV220W v1.14.71 historical RX-IQ regression compatibility hotfix

The first v1.14.70 upgrade installed and verified the protected platform, then aborted in `test_v11435_stock_rxiq_dispatch_correction.py`. The historical test searched the current collector source for the exact human-facing string `RX-path localization requested`. v1.14.70 legitimately replaced that warning when the workflow advanced from RX localization to retained-calibration/passive-RX qualification and controlled active-scan TX testing.

This release restores `v1.14.66 RX-path localization requested` only as a source comment in the current calibration block. It therefore satisfies pinned historical source-contract tests without reintroducing an obsolete user message or changing executable behavior. The current CA/channel-8 restriction, `--ack-rf`, `--ack-calibration`, SSH target invocation, v1.14.70 active-scan gate, b43 patch, RV220W PCI INT0 routing patch and persistent platform remain unchanged.
