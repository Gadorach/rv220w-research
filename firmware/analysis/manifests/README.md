# Structured firmware-analysis manifests

This directory contains machine-readable metadata for the verified 32 MiB image, partitions, kernel ELF, SquashFS superblock, recovered firmware header, JFFS2 tree, and environment/tail regions.

The canonical source image and extracted contents are present elsewhere under `firmware/`; they are excluded only from the core Git bundle and supplied in data overlays.
